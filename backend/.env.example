# Copy to .env and adjust for local dev or production

# Comma-separated frontend URLs allowed to call this API
CORS_ORIGINS=http://localhost:3000

# SQLite database file (relative to backend/)
# DATABASE_URL=sqlite:///./duolingo.db

# Default learner when auth is simplified (assignment allows this)
DEFAULT_USER_ID=1
